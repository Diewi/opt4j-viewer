dependencies {
	compile project(':opt4j-core')
}

sourceSets {
    main {
        java {
            srcDir 'src'
            exclude '**/Opt4JViewerActivator.java'
            exclude 'test/**'
       }
   }
}

javadoc {
   exclude 'ptolemy/**'
}